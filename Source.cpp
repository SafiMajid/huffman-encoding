
#include"Classes.h"







int main()
{
	/*fstream file;
	file.open("file.txt");
	treelist alpha;
	char A; string the_string="";
	file.get(A);
	the_string= the_string + A;
	alpha.insertend(A, 1);
	while (!file.eof())
	{
		file.get(A); the_string = the_string + A;
		if (alpha.searchn(A))
		{
			alpha.edit(alpha.search(A));
		}
		else
		{
			alpha.insertend(A, 1);
		}


	}
	alpha.editc(alpha.search(A));
	the_string.erase(the_string.length()-1,the_string.length());
	
	alpha.huffman_tree();
	alpha.bin_codes.map_print_check();
	file.close();
	cout << endl << the_string<<endl;

	string encoded_stream = "";
	for (int i = 0; i < the_string.length(); i++)
	{
		encoded_stream += alpha.bin_codes.map_to_string(alpha.bin_codes.search(the_string[i]));

	}
	cout << endl << encoded_stream << endl;
	alpha.input_stream_string(the_string,encoded_stream);
	while (alpha.encoded_stream != "")
	{
		alpha.decoded_stream += alpha.string_to_char(alpha.first);
	}
	cout << endl << alpha.decoded_stream<<endl;
*/
	code A("file3 - Copy.txt",true);
	system("pause");

}
